def this_is_gitignored():
    """
    Ignored by our .fake-gitignore file (which is configured as a file to get 
    ignore patterns from in the navigateTo.ignoreFileNames setting in .vscode/settings.json).
    """
    pass
